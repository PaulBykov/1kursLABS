1) АРгументы со значением по умолчанию указываются в последнию очередь, через равно указывается значение
2) по значению?
3) ну тут понятно
4) 
__cdecl - это соглашение о вызове функций, при котором вызывающая функция очищает стек после вызова.

__stdcall - это соглашение о вызове функций, при котором вызываемая функция очищает стек после вызова.

__fastcall - это соглашение о вызове функций, при котором первые два параметра передаются через регистры.

5) формальному аргументу присваивается значение равное переданному значению
6) формальный аргумент представляет собой ссылку на переменную, передаваемую в качестве аргумента