1) программа, подготавливающая код программы на языке C/C++ к компиляции.
2) include, define, undef, pragma, if, else, endif error, line
3) # - помещает элемент за которым идет в двойные ковычки:
	#define fun(s) #s         // "s"
   ## - служит для конткатенации двух строк:
	#define concat(a, b) a ## b
4) статическая и динамическая
5) Пространство имен это scope:: в котором существуют указанные в нем элементы
6)  Это означает, что если тип данных, указанных в инструкции catch, соответствует типу данных исключения, то только эта инструкция catch и будет исполнена.
7) ХЗ
8) -
9) new и delete
10) Стек используется для статичного выделения памяти.
11) #if #else
12) -