x = [0.5, 1, 1.2, 2, 2.9, 3, 4, 4.6, 5];
y = [-6, -10, -12, -15, -16, -20, -22, -25, -30];
n = 10;
plot(x,y);
grid on

a1 = (sum(x) * sum(y) - n * x * y') / (sum(x) ^ 2 - n * sum(x .^ 2));
a0 = 1 / n * (sum(y) - a1 * sum(x));

y1 = a0 + a1 * x;

hold on
plot(x, y1);


legend('Исходная функция', 'Аппроксимирующая функция');

