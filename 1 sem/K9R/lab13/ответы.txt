1)XSLT — язык преобразования XML-документов. Спецификация XSLT входит в состав XSL и является рекомендацией W3C
2) как содержимое-источник должно быть стилизовано,
расположено и разбито на странице в Web-браузере
3, 4)<xsl:template> указывает, как должны преобразовываться
части документа XML. Значение «/» атрибут match используется,
чтобы определить шаблон для всего XML документа целиком
5) <?xml-stylesheet type="text/xml" href="2.xsl"?>
6) Для импорта стилей
7) применяет некий шаблон к текущему элементу или к дочернему узлу текущего элемента
8) <xsl:sort> используется для сортировки выходных данных и
располагается внутри элемента <xsl:for-each> 
9) select - указание ключа
	order - порядок сортировки
	lang - язык ключей сортировки
10, 11) if
12) XSL-трансформация, XSLT, XSL-FO
15) <xsl:value-of> используется для извлечения значения отобранного XML элемента и добавления его в выходной поток преобразовываемого документа.
16)<xsl:for-each> может использоваться для выбора каждого
XML элемента заданного узлового набора
